# SPEC.md — hanshichi-atlas(半七アトラス)

<!-- scaffold template v1.24.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

青空文庫に収録された岡本綺堂『半七捕物帳』全 69 話を、**一話の中に二つの時間が同居している**
という一点から俯瞰する静的 Web アプリ。

半七物はすべて聞き書きの形をとる。明治の「わたし」が老いた半七を訪ね、半七が江戸の事件を語る。
したがって各話には「語る現在(明治)」と「語られる過去(江戸)」があり、本文はその両方に元号を書く。
本アプリはこの二層を分離して並べ、次の三面で見せる。

| 面 | ページ | 見せるもの |
|---|---|---|
| 年表(背骨) | `web/index.html` | 連載順と事件年代順の二軸。明治の語りの帯と江戸の事件の帯 |
| 捜査圏(第二面) | `web/map.html` | 神田三河町を中心とした事件地・足取りの配置 |
| ルビの地層(第三面) | `web/lens.html` | 大正の読者に「読めまい」と判断された語の分布 |
| リーダー | `web/reader.html` | ルビ表示付き本文 |

### 取り下げた目玉(測って捨てた記録 — L1 で決着)

当初の目玉は **「半七の生年は本文の中で矛盾する」** だった。根拠として次の二つを挙げていた。

- 01「お文の魂」:「わたしが丁度二十歳の時だから、元治元年」→ 1864 年に 20 歳(生年 1845)
- 47「金の蝋燭」:「その安政二年はわたくしが三十三の年」→ 1855 年に 33 歳(生年 1823)

**この主張は成立しなかった。**01 の「二十歳」は半七の言葉ではない。本文はその直後に
「と、おじさんは先ず冒頭を置いた」と書いており、話者は語り手の叔父(Ｋのおじさん)である。
抽出した一文だけを見て話者を確かめなかったのが誤りだった(L1 の failure ログ CTX-MISS を参照)。

半七に帰属する言明を全数当たると、三つとも同じ生年に収束する(実測 2026-08-31):

| 話 | 言明 | 指す年 | 年齢 | 生年(数え年) |
|---|---|---|---|---|
| 02 石灯籠 | 「天保丑年の十二月で、わたくしが十九の年の暮でした」 | 1841 | 19 | 1823 |
| 47 金の蝋燭 | 「その安政二年はわたくしが三十三の年で」 | 1855 | 33 | 1823 |
| 01 お文の魂 | 「半七は七十を三つ越したとか云っていた」(日清戦争終結の頃) | 1895 | 73 | 1823 |

G-05 の規定に従って目玉を取り下げ、外れた予測はここに残す。

### 目玉の主張(差し替え後)

**半七の年譜は本文だけで一意に決まり、綺堂の年代表記は内部で破綻していない。**

上の三経路が生年 **文政六年(1823)** に収束する。さらに、本文が元号年と干支の両方を書いている
箇所を照合すると **6 件すべてが一致する**(実測 2026-08-31。18・31・37×2・65・69)。
換算表を 1 年ずらすと 6 件すべてが落ちるので、この照合は素通しではない。

この一意に決まる年譜は、単なる感心事ではなく **F-07 の検算になる**。半七が自ら手がけた事件は
生年より後でなければならないから、事件年の確定に外部の正解を持ち込まずに枠を掛けられる(G-08)。

主張はすべて本文の内部で閉じる —— 外部の年譜・研究文献を正解として持ち込まない。
元号から西暦への換算のみ外部の元号表に依る(G-03 参照)。

## 2. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | 青空文庫の公式一覧 `list_person_all_extended_utf8.zip` から作品名「半七捕物帳」の全行を抽出し、`data/aozora_works.json` に記録する。採録対象は文字遣い種別が新字新仮名のもの。副題の先頭 2 桁を通番とする。旧字旧仮名版・随筆「半七捕物帳の思い出」(作品 ID 49532)は series 外として別枠に記録し、本文取得の対象としない | must | L1 |
| F-02 | 各話の「XHTML/HTML ファイル URL」から本文を取得し `data/raw/` に保存する。全ファイルに取得元 URL と取得日を記録する。取得間隔は 1 秒以上、再実行はローカルキャッシュ優先で再取得しない | must | L1 |
| F-03 | 青空文庫 XHTML パーサー: 底本情報(`bibliographical_information`)の分離、ルビ(`<ruby>`/`<rt>`/`<rp>`)、入力者注(`[#…]`)、見出しを構造化した JSON を出力する | must | L1 |
| F-04 | 往復検査: パース → 再直列化で `main_text` 領域が原文と完全一致する(取得済み全 69 話) | must | L1 |
| F-05 | 本文中の元号年表記を全数抽出し、`data/eras.json` に位置・文脈付きで記録する。**抽出器を書く前に全 69 話で被覆を測り**(HC-069)、取りこぼしが 0 件であることを実測してから期待値を書く | must | L1 |
| F-06 | 半七の年齢・生年に関する言明を全数抽出し、`data/hanshichi_age.json` に記録する。目玉の主張(§1)の成否をここで判定する | must | L1 |
| F-07 | 各話の事件年を確定する。本文の元号年は語りの現在(明治)・事件の年・作中人物の回想が混在するため、単一の規則で決め打ちしない。**決定は本文を読んで手で下し、根拠の文は本文から機械で切り出す**(書き写さない)。台帳の年は本文から抽出した候補の集合に閉じ込め、候補外の年を採るときは導出を明示する。確定できない話は **`null` のまま出荷する**(推定で埋めない)。**一話に事件が二つある話がある**(10 / 34 / 39)ので、事件年は単一値でなく列で持つ。本文自身が年を決めかねている話(17「文久三年か元治元年」)、精度を放棄している話(38「安政の末年」)は不確実として印を付ける | must | L2 |
| F-08 | 年表ビュー(`web/index.html`): 同じ目盛りの上に江戸の事件と明治の語りを並べた二層の年表、半七の生涯の帯、連載順 / 事件年代順の切替、分類の絞り込み、相互参照と共有出来事の表。**レーンの高さは同じ年に重なる件数から導く**(定数で決め打ちすると点がはみ出す)。配色は dataviz の検証器に掛けた値(光・暗の両モードで全項目通過)。狭い画面では図だけを自分の器の中で横スクロールさせ、点を潰さない | must | L3 |
| F-09 | 捜査圏の地図(`web/map.html`): 江戸地名の同定と配置。地名→座標の同定は kiko-atlas の辞書照合方式に倣い、同定できない地名は地図に出さず一覧に残す | should | L4 |
| F-10 | ルビの地層(`web/lens.html`): ルビ語彙の分布と話ごとの密度 | should | L5 |
| F-11 | リーダー(`web/reader.html?w={no}`): ルビ表示付き本文 | must | L3 |
| F-12 | 相互リンク統合・Vercel デプロイ(本番 hanshichi-atlas.vercel.app)・app-menu 掲載・GitHub 公開 | must | L6 |
| F-13 | フリート共通フッタ(全ページ・下部固定): © / GitHub / 歩き方 / 設計図 / App Menu | must | L6 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的サイトとして動作(サーバー・API 不要)。データはビルド時生成の JSON | 成果物の構成確認 |
| N-02 | 青空文庫への負荷配慮: 取得間隔 1 秒以上、User-Agent 明示、再実行はキャッシュ優先で再取得しない | fetch スクリプトのコード確認 + 再実行時の HTTP アクセス数 0 |
| N-03 | 全データに出所(URL)と取得日を記録する | データファイルの provenance フィールド検査 |

## 4. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | **件数オラクル**: 採録した話の通番集合が `{01,…,69}` と一致する(欠番・重複なし)。件数を定数で書かず集合一致で書く(HC-016) | L1 |
| G-02 | **往復検査**: 全 69 話で `main_text` が原文と完全一致 | L1 |
| G-03 | **循環の禁止**: 元号 → 西暦の換算表を本文から作らない。外部の公的元号表を独立に持ち、出所を `data/era_table.json` の provenance に明記する。本文の年号解釈をこの表の検証に使ってはならない | L1 |
| G-04 | **未分類 0 件**: 全 69 話が「事件年を確定した」「確定できない(理由付き)」のいずれかに分類され、どちらでもない話が 0 件 | L2 |
| G-05 | **目玉ゲート**(判定済み・L1): 半七の生年について本文由来の独立な言明が 2 件以上あり、それらが両立しない。→ **不通過。**三件とも 1823 に収束したため、当初の目玉を取り下げ、収束の側を目玉に差し替えた(§1)。判定は T-402 が固定する | L1 |
| G-08 | **年譜の枠**(判定済み・L2): 半七が自ら手がけた事件の年は、半七の初陣(1841)以降である。→ **通過。**この不等式が 5 話(18・24・33・65・69)を選り分け、読み返すといずれも半七の探索ではなかった(69「白蝶怪」は半七の親分・吉五郎の世代の事件で、半七は登場しない)。分類「半七以前の聞き伝え」を新設した | L2 |
| G-09 | **相互参照の照合**: 話どうしが題名で参照し合う 22 箇所のうち、参照と同じ文の中に年の主張があるものを、参照先の事件年と突き合わせる。窓を文字数で切ると無関係の年を拾って誤って不一致を出すので、**同じ文**に限る。取りこぼす側に倒す | L2 |
| G-10 | **同じ出来事の日付照合**: 別々の話が同じ江戸の出来事に触れる箇所で、日付が揃っているか。比べる主張は明示し(語で一括りにすると別の出来事が混ざる)、日付はその文から機械で取る | L2 |
| G-06 | **陽性対照**: 元号抽出器・年齢抽出器のそれぞれに、捕まえるべき既知の例と撃ってはならない例を対で置く(HC-041) | L1 |
| G-07 | **字種検査**: `python harness/text_hygiene.py` が通る。ただし本文データ(`data/raw/`・`data/parsed/`)は青空文庫の原文であり検査対象外とする | 全ループ |
| G-11 | **実ブラウザ検品**: 図・表に手を入れたループは `python harness/inspect_web.py` を通し、**実際に目で見る**。検品器は 3 つの幅(1280 / 760 / 390)で開き、横の溢れ・縦の伸びすぎ・図の縦横比・吹き出し・並べ替え・ルビの切替を振る舞いで確かめる。`--control` で壊した木に当て、検品器自身が発火することを確かめる | 図を触ったループ |

## 5. スコープ外

- 半七物以外の岡本綺堂作品の本文取得(一覧への記録も行わない)
- 本文の校訂・改変(取得したままを正とする)
- 外部の研究文献・年譜を正解として持ち込むこと(§1 の理由による)
- 事件年が確定できない話を推定で埋めること(F-07)
