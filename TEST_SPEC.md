# TEST_SPEC.md — hanshichi-atlas

<!-- scaffold template v1.24.0 から展開(2026-08-31) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- `validation` は取得済みコーパス(`data/raw/`)を前提とする。未取得環境では skip
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `data/aozora_works.json` | 青空文庫 `list_person_all_extended_utf8.zip`(https://www.aozora.gr.jp/index_pages/list_person_all_extended_utf8.zip、2026-08-31 取得) | 実データ。改変禁止 |
| 通番オラクル(T-101) | コーパス自身が持つ手がかり —— 副題の先頭 2 桁が連番になっている。外部の「全 69 話」という定数に頼らず、**集合 `{01,…,69}` との一致**で書く | 自己完結オラクル |
| 往復検査(T-201) | パーサー自身の逆写像。原文が正解であり外部正解不要 | 自己完結オラクル |
| `data/era_table.json` | 元号 → 西暦の対応。国立国会図書館「日本の暦」ほか公的資料を独立に転記し、provenance に出所と転記日を書く。**本文から作らない**(G-03) | 外部権威 |
| 元号抽出の被覆(T-301) | 全 69 話に対する実測。抽出器を書く前に測り、取りこぼし 0 件を確かめてから期待値にする(HC-069) | 実測 |
| `tests/fixtures/era_positive.txt` / `era_negative.txt` | 本文からの抜粋(捕まえるべき例)と、撃ってはならない正当な例。G-06 の陽性・陰性対照 | 実データ抜粋 + 対照 |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | F-01 / G-01 | 採録した話の通番集合 | `{01,…,69}` と一致(欠番・重複なし) |
| T-102 | F-01 | series 外の分離 | 旧字旧仮名版・随筆(ID 49532)が本文取得対象に入らない |
| T-103 | F-01 / N-03 | provenance | 全エントリに取得元 URL と取得日がある |
| T-104 | F-02 | キャッシュ優先 | 取得済み状態で再実行すると HTTP アクセス 0 |
| T-201 | F-04 / G-02 | 往復検査(フィクスチャ) | `serialize(parse(x)) == x` |
| T-202 | F-04 / G-02 | 往復検査(全 69 話) | 全話で `main_text` が原文と完全一致。1 件でも不一致なら L1 未完了 |
| T-203 | F-03 | ルビ抽出 | `<ruby><rb>base</rb><rp>(</rp><rt>yomi</rt><rp>)</rp></ruby>` から base/yomi を取り、再直列化で復元される |
| T-204 | F-03 | 入力者注 | `［＃…］` を verbatim 保存する |
| T-301 | F-05 / G-06 | 元号抽出の陽性対照 | `era_positive.txt` の既知の元号年をすべて捕まえる |
| T-302 | F-05 / G-06 | 元号抽出の陰性対照 | `era_negative.txt`(「明治座」「文久銭」など元号語を含むが年ではない例)で 0 件 |
| T-303 | F-05 | 抽出の全数性 | 全 69 話の走査で例外を出さず、走査対象が空でない |
| T-304 | G-03 | 換算表の独立性 | `era_table.json` の provenance が本文でないこと、および換算関数が本文データを参照しないこと |
| T-401 | F-06 / G-05 | 年齢言明の抽出 | 01・47 の既知 2 件を捕まえる(実測 2026-08-31) |
| T-402 | F-06 / G-05 | 目玉ゲート | 抽出した言明から導かれる生年が 2 通り以上あり、差が数え年の幅(±2 年)を超える |
| T-901 | G-07 | 字種検査 | `harness/text_hygiene.py` が通る(`data/` 配下は対象外) |
